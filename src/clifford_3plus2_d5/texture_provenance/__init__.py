"""A3b — texture provenance ("derive or count").

A3a (`unified_boundary/`) unified the transfer boundary `H_Q`. A3b is the honest
"derive or count" audit of the CKM/PMNS texture factors — the place where theory
vs numerology is decided.

The ledger is genuinely mixed, and that is the result:

* Derived (group-theory / geometry, machine-checkable): the color return
  ``C_F = sum_A T^A T^A = 4/3`` (SU(3) Casimir); the coin base ``sqrt(5)`` from
  ``Gamma_q^2 = 5 I`` with ``5 = 2_BCC + 3_color``; the BCC Clebsches ``sqrt(2)``
  and ``1/sqrt(2)``; the charged-lepton ``sqrt(3/2)`` from the residual port; the
  leptonic phase *word* selection from V10 boundary-loop holonomy.  These give the
  texture STRUCTURE — the CP phases ``atan(sqrt(5))`` and ``5 pi / 12`` and the
  PMNS angle structure.
* Free input (4): the quark depth embedding ``{0,2,6}`` (fit to the CKM
  hierarchy); the charged-lepton two-step depth; the flat-coin ``r=1`` ergodicity
  prior; the CP-phase branch.  These set the HIERARCHY.

Verdict ``TEXTURE_STRUCTURE_DERIVED_HIERARCHY_INPUT``: the framework predicts the
structure, conditional on the free depth embedding, with fewer free inputs than
observables (not numerology).  Deriving the depth embedding itself is a generation
mechanism (open; ``N=3`` is empirical per the closed kill-sidecars) and is
recorded as the remaining input, not attempted here.
"""
