# Session 18 - Down Odd-Shell Rank-Five Audit

Session 07 left the down bottom coefficient as a rank-five ambiguity:

```text
regular S3 can host rank 5, but S3 alone has two rank-five complements.
```

Session 18 uses the active primitive shell from Session 17 to sharpen this.

## Primitive Predicates

Inside the active six-label quark shell:

```text
1_even + 5_odd = 1_direct + 2_BCC + 3_color
```

the down candidate counts are exact primitive predicates:

```text
d: full primitive shell = 6
s: BCC odd doublet      = 2
b: full odd shell       = 5
```

Thus

$$
C_d=\left(1,\frac1{\sqrt3},\sqrt{\frac56}\right).
$$

The rank-five line is no longer an arbitrary $S_3$ complement.  It is the
primitive parity projector:

```text
odd shell = regular primitive shell - even direct line.
```

The strange middle line is likewise not the color triplet; it is the BCC odd
doublet.  The color-only middle control is rejected.

## Remaining Premise

This still does not derive the physical down readout from first principles.  It
derives the candidate **given** the down-head assignment:

```text
bottom reads the full primitive odd shell
```

That is a sharper premise than "choose a rank-five S3 complement", but it is
still a premise.

## Verdict

```text
QUARK_DOWN_ODD_SHELL_RANK_FIVE_CONDITIONAL_PASS
```

Meaning:

```text
The active primitive shell removes the S3 rank-five ambiguity.  The bottom
candidate is the full primitive odd shell, while the strange channel is the BCC
odd doublet.  The remaining theory burden is the physical down-head readout
that assigns bottom to the odd shell.
```

Certainty: `C:9` for the primitive subset counts and controls; `C:5` for the
bottom coefficient as a physical prediction until the down odd-shell readout is
derived from the microscopic boundary dynamics.
