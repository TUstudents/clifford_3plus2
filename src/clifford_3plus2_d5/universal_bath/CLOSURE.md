# universal_bath - Closure Note

Status: **PARKED after Session 24**.

This sidecar should be closed for now.  It produced useful physics and useful
negative information, but the quark program is now blocked by dynamical
premises rather than by missing bookkeeping.

## What Survived

The central architecture is still valuable:

```text
sector response = finite source head + universal retarded silver tail
```

The common bath spine, silver terminator, Jacobi/CMV taxonomy, selected active
plane, retarded compression model, and source-ledger discipline are reusable.

The neutrino sector remains the strongest prototype:

```text
Sigma_nu = epsilon^2 P_u + P_b
Delta m^2_21 / Delta m^2_31 = epsilon^4
```

inside the selected/product-family bath.  The needed microscopic upgrade is
still the derivation of the selected family-port graph from the BB boundary
material, not a new flavor fit.

The charged-lepton work also clarified what is real:

- `2/9` is an exact source occupation moment;
- Koide equipartition is realized by the minimal two-sided graph;
- the microscopic trace-path origin and occupation-to-angle dynamics remain
  open.

The quark work reduced several vague choices to sharp statements:

- the source line `b` is the selected-$S_2$ odd current line;
- first passage from `b` gives the desired depth ordering `(2,1,0)`;
- the up Taylor head follows from `exp(N/sqrt(2))b`;
- the down rank-five bottom coefficient is selected inside the non-contact
  retarded-current model.

## Why We Stop

The sidecar now hits a genuine obstacle:

```text
H_tilde -> retarded/oriented flag N
H       -> Hermitian closure Delta_N
```

is **not derived** by the available symmetry and boundary-current data.

Session 24 shows the obstruction exactly:

```text
R N R = N.T
R Delta_N R = Delta_N
R N R != Delta_N
```

Higgs conjugation / endpoint reflection gives the reverse flag, not the
Hermitian closure.  The down readout needs an additional pairing dynamics.
The declared assignment and the swapped assignment both survive the current
constraints.

Therefore continuing to CKM or more quark prefactors inside this sidecar would
mostly add conditional structure on top of a missing dynamical bit.

## Parked Gates

Do not present these as solved:

```text
higgs_door_orientation_couples_H_tilde_to_retarded_flag_and_H_to_flag_closure
down_mass_event_requires_nonidentity_hidden_excursion_before_return
colored_quark_mass_source_is_selected_S2_odd_boundary_current
equal_boundary_degeneracy_or_max_entropy_prior
single_clock_locking_field_is_realized_by_boundary_material
mixed_normal_clock_error_ports_are_outgoing_asymptotic_leads
active_cmv_torsion_angle_2_over_9_is_generated_by_boundary_dynamics
```

The first item is the main quark blocker.  The last three are broader boundary
material / charged-lepton blockers.

## How To Reopen

Reopen only with a new physical principle, not another audit session.  The
right reopening target is a microscopic boundary-material or Higgs-boundary
dynamics that forces one of the parked gates, especially:

```text
direct H door -> paired Hermitian current covariance
conjugate H_tilde door -> one-way retarded flag
```

If such a principle is found, the current certificates provide the finite
algebraic checks.  Until then, `universal_bath` is best treated as a clear
ledger of what the universal-tail architecture explains and where quark flavor
requires new dynamics.

## Certainty

- `C:9` for the exact finite algebra and controls in the session certificates.
- `C:8` that the current quark route is blocked by the Higgs-door
  orientation-coupling premise.
- `C:6` for retaining the universal-tail architecture as a useful organizing
  principle.
- `C:3` for claiming a complete quark flavor derivation from this sidecar.
