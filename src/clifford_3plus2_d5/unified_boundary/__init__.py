"""A3a — unified transfer boundary.

Roadmap gate A3a. A2 established the *necessary conditions* for one-boundary
universality; A3a constructs the unification of the transfer sector.

Decisive reframe: the quark sector has no Schur self-energy — its CKM is
assembled combinatorially (flat Cl_5 coin phase atan(sqrt(5)) + transfer depths
epsilon^k + color/BCC Clebsches), while the neutrino core ``K_nu = eps^2 P_u +
P_b`` is a genuine Schur complement ``Sigma = V^dagger (z - H_Q)^{-1} V`` of an
explicit sterile chain ``H_Q``.  A3a puts the quark *transfer* sector on the same
Schur footing as the lepton sector using the *same* ``H_Q``: the quark transfer
amplitudes epsilon^2, epsilon^4, epsilon^6 are powers of the *same* sterile chain
Weyl factor that drives the neutrino core.

Honest scope: A3a unifies the transfer boundary ``H_Q`` and shows the
sector-specific structure (color C_F, BCC Clebsches, coin phase) lives in the
coupling ``V_f``.  It does NOT derive the quark depths {0,2,6} or the Clebsch /
coin factors from the chiral-16 geometry — those remain inputs whose derivation
is A3b (deferred).  The gate can KILL but does not prove the full flavor pattern.

Gates:
* A3-1 — one ``H_Q`` (sterile chain); the lepton ``Sigma = eps^2 P_u + P_b`` is
  its Schur complement.
* A3-2 — the quark transfer hierarchy is the *same*-chain Schur/Green structure
  (amplitudes = (common chain factor)^depth).
* A3-3 — the remaining quark structure (color C_F=4/3, BCC sqrt(2)/(1/sqrt(2)),
  coin phase atan(sqrt(5))) is ``V_f`` quantum-number data, not ``H_Q``.
"""
